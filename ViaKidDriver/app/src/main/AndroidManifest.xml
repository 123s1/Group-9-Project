<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 定位权限 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- 注意：后台定位需要向 Google Play 提交使用说明，证明是核心功能 -->
    <uses-permission
        android:name="android.permission.ACCESS_BACKGROUND_LOCATION"
        tools:ignore="BackgroundLocationPolicy" />

    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- 相机权限 -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <!-- 存储权限说明：
         - Android 9 及以下：使用 READ/WRITE_EXTERNAL_STORAGE
         - Android 10-12：使用 scoped storage，不需要存储权限即可访问自己的文件
         - Android 13+：使用媒体特定权限（READ_MEDIA_*）
         - WRITE_EXTERNAL_STORAGE 在 Android 10+ 已废弃，建议使用 MediaStore API 或 SAF
    -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- WRITE_EXTERNAL_STORAGE 已废弃，如需写入共享存储请使用 MediaStore API -->

    <!-- 媒体权限（Android 13+）
         注意：如果只需要让用户选择照片/视频，建议使用 ActivityResultContracts.PickVisualMedia
         而不是申请这些权限，这样无需向 Google Play 说明理由
    -->
    <uses-permission
        android:name="android.permission.READ_MEDIA_IMAGES"
        tools:ignore="PhotoAndVideoPolicy,SelectedPhotoAccess" />
    <uses-permission
        android:name="android.permission.READ_MEDIA_VIDEO"
        tools:ignore="PhotoAndVideoPolicy,SelectedPhotoAccess" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- 震动权限 -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- 麦克风权限 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- 前台服务（GPS上报）
         Android 14+ 需要声明前台服务类型并在运行时提供通知
    -->
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE"
        tools:ignore="ForegroundServicesPolicy" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <application
        android:usesCleartextTraffic="true"
        android:name=".ViaKidApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ViaKidDriver">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ViaKidDriver">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
